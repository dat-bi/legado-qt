<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?android:attr/colorBackground"
    android:animateLayoutChanges="true">

    <io.legado.app.ui.widget.TitleBar
        android:id="@+id/title_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:transitionName="title_bar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:orientation="vertical"
            android:animateLayoutChanges="true">

            <!-- Item: Names -->
            <include
                android:id="@+id/item_names"
                layout="@layout/item_dict_manage"
                android:transitionName="names_item" />

            <!-- Item: VietPhrase -->
            <include
                android:id="@+id/item_vietphrase"
                layout="@layout/item_dict_manage"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:transitionName="vietphrase_item" />

            <!-- Item: PhienAm -->
            <include
                android:id="@+id/item_phienam"
                layout="@layout/item_dict_manage"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:transitionName="phienam_item" />

        </LinearLayout>
    </ScrollView>

</LinearLayout>